# START.md — текущее состояние + указания агентам (обновлено 2026-08-16)

## Где мы
- Итерация 3 + фиксы UX + **умный сим** применены и запушены (`4936f49`).
- **Текущий результат (детерминированный прогон): НОРМА 61 [B] · ХАОС 48 [C]**
- Пиромант 58 · Некромант 58 · Штормовой 66 · Архимаг 62 (все p50 10:00).
- Золото: доход ~3700/забег, траты ~3300, 14 артефактов (эпики покупаются).

## Как продолжить сессию с нуля
1. `git pull` (ветка main).
2. `node --check sim\sim-model.js; node sim\node_runner.js` → убедись в НОРМА 61 [B].
3. Прочитай `.opencode\wiki\balance-sim-2026-08-16.md` (модель + уроки) и
   `Data\Отчёт_2026-08-16_баланс-итерация3_умный-сим.md` (полный отчёт).
4. Текущие параметры balance.js: blob.hp:19, hpGrowth:.04, hpCurve:.05, dmgGrowthT:500,
   spawnStart:.7, spawnEnd:.45, healOnLevel:15, bosses projDmg:18/projCountBase:5, towers projDmg:8/projCount:3.

## Цель (TESTING.md): norma≥75, chaos≥60, каждый класс≥60
Сейчас 61 — до цели остались: некромант (▼ 1м/3м/8–10м, soulreaper в симе не добирается),
пиромант (▲ 2м/6–7м — мид-перекос). ПРЕДУПРЕЖДЕНИЕ: буст комбо-выбора ×7 ломает старт шторма
(lightning моно-качание → смерть 1:30) — не применять вслепую.

## Живой тест (за человеком)
- Запуск: `py serve.py` → http://localhost:8000/index.html
- Забег на НОРМА 10:00; логи автоуходят в `Data\run-log.json` (POST /api/log),
  счётчик в оверлее «Game Over»/«Победа».
- Сверить: живой игрок слабее сима → калибровка под сима может сделать игру тяжелее.

## Запрещённое
- Менять game.js/render.js/combos.js под баланс — только balance.js/spells.js/sim/*.
- Убирать сид/фикс-набор eff/foe в runSuite — иначе прогоны несравнимы.
- Менять критерии TESTING.md без согласования с пользователем.