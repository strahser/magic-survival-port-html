# Ответы Qwen (chat.qwen.ai)

**Чат:** 4c64d6e2-c94d-4b8f-aca3-974d87c20ede

**Дата:** 2026-08-16T15:37:22

---

## Ответ 1

Подтверждаю ваш анализ — это классические «качели»: убрали ранний спайк, но `spawnStart 1.15` обрушил спавн-пул и дал перебор (▲ 2–5м), а шторм/аркана не тянут средний DPS в ХАОС. Итерация 2 — возвращаем давление в коридор и точечно поднимаем отстающие школы (в пределах ±15%).

---

### `balance.js`
```js
  spawnStart:1.05,spawnEnd:.45,spawnRampT:480,groupMax:2,groupRampT:150,
```

### `spells.js` (Шторм — средний DPS)
```js
 lightning:{name:'Цепная молния',icon:'⚡',cd:1.35,lv:[{dmg:18,chains:2},{dmg:26,chains:3},{dmg:36,chains:4},{dmg:49,chains:5}]},
```
```js
 cyclone:{name:'Смерч',icon:'🌀',lv:[{n:1,r:60,dmg:11,pull:90},{n:2,r:66,dmg:16,pull:110},{n:2,r:74,dmg:21,pull:130},{n:3,r:80,dmg:27,pull:150}]},
```
```js
 ball:{name:'Шаровая молния',icon:'🔮',cd:2.8,lv:[{n:1,dmg:12,r:15},{n:2,dmg:18,r:17},{n:3,dmg:24,r:19}]},
```

### `spells.js` (Аркана — средний DPS)
```js
 arcane:{name:'Чародейские стрелы',icon:'✨',cd:1,lv:[{dmg:12,n:1},{dmg:11,n:2},{dmg:14,n:3},{dmg:19,n:4},{dmg:24,n:5}]},
```
```js
 icelance:{name:'Ледяное копьё',icon:'❄️',cd:1.7,lv:[{dmg:20,n:1,pierce:5},{dmg:31,n:2,pierce:6},{dmg:46,n:2,pierce:9},{dmg:61,n:3,pierce:10}]},
```
```js
 ray:{name:'Лучи хаоса',icon:'🔆',lv:[{n:2,dmg:7,range:250},{n:3,dmg:8,range:270},{n:4,dmg:9,range:290},{n:5,dmg:11,range:310}]},
```

---

### Ожидание и следующий рычаг
- `spawnStart 1.05` вернёт спавн-пул → ▲ на 2–5м у пиро/некро уйдут в коридор (НОРМА ↑).
- +10% шторму/аркане закроет их ▼ на 1–3м и поднимет ХАОС p50.
- Прогоните `node sim/node_runner.js`, пришлите `ИТОГ` + теплокарты.

**Если после этого НОРМА 60–74 (близко, но не ≥75):** следующий рычаг — `hpCurve 0.04→0.045` (чуть больше лейт-HP, чтобы сгладить остаточные ▲) при неизменном остальном. **Если шторм/аркана всё ещё ▼ в ХАОС:** дам им по ещё +10% ядру (в пределах лимита на итерацию).

Жду отчёт.

---

## Ответ 2

Подтверждаю комбинацию (с поправкой: `hpGrowth` сейчас 0.02, а не 0.045) + закрываю ваш батч багов/UX. Итого итерация 3 + фиксы.

---

## Баланс-итерация 3
### `balance.js`
```js
  hpGrowth:.024,hpCurve:.045,dmgGrowthT:600,armorCap:12,spdGrowth:900,
```
```js
 bosses:{times:[70,180,300,430],hp:[450,1100,2200,3600],dmg:24,spd:46,projDmg:18,shootEvery:3.2,projCountBase:5},
 towers:{hp:180,gold:45,heal:30,xp:10,projDmg:8,shootEvery:3,projCount:3},
```
### `spells.js` (шторм/аркана +10% на lv1-2)
```js
 lightning:...lv:[{dmg:20,chains:2},{dmg:29,chains:3},{dmg:36,chains:4},{dmg:49,chains:5}]},
 cyclone:...lv:[{n:1,r:60,dmg:12,pull:90},{n:2,r:66,dmg:18,pull:110},{n:2,r:74,dmg:21,pull:130},{n:3,r:80,dmg:27,pull:150}]},
 ball:...lv:[{n:1,dmg:13,r:15},{n:2,dmg:20,r:17},{n:3,dmg:24,r:19}]},
 arcane:...lv:[{dmg:13,n:1},{dmg:12,n:2},{dmg:14,n:3},{dmg:19,n:4},{dmg:24,n:5}]},
 icelance:...lv:[{dmg:22,n:1,pierce:5},{dmg:34,n:2,pierce:6},{dmg:46,n:2,pierce:9},{dmg:61,n:3,pierce:10}]},
 ray:...lv:[{n:2,dmg:8,range:250},{n:3,dmg:9,range:270},{n:4,dmg:9,range:290},{n:5,dmg:11,range:310}]},
```

---

## Баги/UX

### 1. Двойник не добавляется (фикс)
В `game.js` добавьте функцию и вызов в `update` (позиции миражей не обновлялись):
```js
function updateMirages(dt){if(!G.spells.mirror&&!G.spells.avatar){G.mirrorPos=[];return;}
 const n=((G.spells.avatar?SPELL_DEF.avatar.lv[0].n:SPELL_DEF.mirror.lv[G.spells.mirror-1].n))+G.stats.mirrorBonus;
 G.mirrorAng+=dt*1.3;G.mirrorPos=[];
 for(let i=0;i<n;i++){const a=G.mirrorAng+i/n*TAU;G.mirrorPos.push({x:G.player.x+Math.cos(a)*56,y:G.player.y+Math.sin(a)*56});}}
```
Вызовите `updateMirages(dt);` рядом с `updateCyclone(dt);`.

### 2. Магазин: вкладки + читаемый верх + цены по доходу
Теоретический доход ≈ 1500–2500 золота/забег → обычное ~70, редкое ~160, эпик ~420–520 (дорого, но достижимо). Замените `shopPrice`:
```js
function shopPrice(a){const base={'I':70,'II':160,'III':420,'КЛАСС':240,'ТИР+':520}[artRank(a)]||a.price;
 return Math.round(base*(1+G.t/600));}
```
Добавьте вкладки. В `renderShop` в начале очистите и постройте `$('shopTabs')` из трёх кнопок (`I` ОБЫЧНОЕ / `II` РЕДКОЕ / `III+` ЭПИК), фильтруйте `inTab(a)` по `artRank`, карточки — по одной, клик по карточке → `showArtRecipes(a)`.

### 3. Сетовые сборки по клику (фикс)
Замените `showArtRecipes` на вывод в `$('shopInfo')` (баннеры перезаписывались):
```js
function showArtRecipes(a){const rs=RECIPES.filter(r=>r.need.includes(a.id)||r.id===a.id);
 let s=a.icon+' '+a.name+': ';
 if(!rs.length)s+='вне сборок';
 else s+=rs.map(r=>{const miss=r.need.filter(n=>!G.artifacts.includes(n)).map(n=>(ARTSHOP.find(x=>x.id===n)||{}).name||n);
  return r.name+(miss.length?' (нет: '+miss.join(', ')+')':' (готово)');}).join(' · ');
 $('shopInfo').textContent=s;}
```
В `index.html` в `shopOv` добавьте `<div id="shopTabs" class="choiceTabs"></div>` перед `#shopItems` и `<div id="shopInfo" class="note"></div>` после.

### 4. Ранжирование пуль (мелкие слабые, крупные смертельные) + меньше их
- Мелкие (башня/стрелок): `r:5,dmg:8` — в `game.js` у башни `r:7,dmg:TO.projDmg` → `r:5`, у стрелка `r:6,dmg:e.dmg` → `r:5,dmg:8`.
- Крупные (босс/страж): `r:9,dmg:BO.projDmg(18)` / страж `r:8,dmg:16`.
- Уменьшите счётчики: башня `projCount:3`, босс `projCountBase:5`, страж `cnt=3+G.bossIdx`, стрелок `shootT=3.2`.

### 5. Сундуки только от боссов
В `director` удалите блок `if(G.chestT<=0&&...){...}` (таймерный спавн). В `kill` у стража уберите `G.chests.push(...)` (оставьте баннер/золото). Награды-артфакты остаются через `openReward(3)` от боссов.

### 6. Карточка синергии (сет) в выборе
В `openChoice` добавьте секцию `{l:'~ СИНЕРГИИ',b:buildSynergyGrid}` и функцию:
```js
function buildSynergyGrid(){const g=document.createElement('div');g.style.cssText='display:flex;flex-wrap:wrap;gap:14px;justify-content:center;width:100%';
 for(const sy of SYNERGIES){const on=synergyActive(sy);
  const needs=sy.need.map(n=>n.type==='passive'?PASSIVE_DEF[n.id].name:SPELL_DEF[n.id].name).join(' + ');
  const el=document.createElement('div');el.className='pickCard'+(on?' up':' disabled');
  el.innerHTML=`<div class="pcIcon">${sy.icon}</div><div class="pcName">${sy.name}</div><div class="evoNeeds"><div class="evoNeed">${needs}</div></div><div class="pcLvl">${on?'АКТИВНА':'—'}</div><div class="pcDesc">${sy.desc}</div>`;
  g.appendChild(el);}
 return g;}
```

---

Прогоните сим и живой тест. Жду `ИТОГ` + теплокарты; если ▲ 8–10м останутся при НОРМА<75 — следующий рычаг `hpCurve 0.045→0.05`.

---

## Ответ 3

Отлично — сим стал честным, и теперь делаем его **умным**, чтобы он играл как осмысленный игрок (приоритеты магии/артефактов) и сам валил баланс без живых прогонов. Плюс чиню несостыковку цен: сим переходит на ту же формулу `shopPrice`, что и живой магазин (и смягчаю рост до `1+t/900`, чтобы эпик был достижим).

---

### `game.js` — смягчить рост цены
```js
function shopPrice(a){const base={'I':70,'II':160,'III':420,'КЛАСС':240,'ТИР+':520}[artRank(a)]||a.price;
 return Math.round(base*(1+G.t/900));}
```

### `sim/sim-model.js` — интеллект

**1.** Добавьте хелперы (вверх файла, после `spellDps`):
```js
function ownerSim(id){for(const k of CLASS_ORDER)if(CLASSES[k].pool.includes(id))return k;return null;}
function simRank(a){if(a.craftOnly)return'СБОРКА';if(a.cls)return'КЛАСС';if(a.req)return'ТИР+';return a.price>=130?'III':a.price>=90?'II':'I';}
function simPrice(a,t){const base={'I':70,'II':160,'III':420,'КЛАСС':240,'ТИР+':520}[simRank(a)]||a.price;return Math.round(base*(1+t/900));}
function artValue(a,st,dps,hpFrac){let v=0;const need=hpFrac<.8;
 if(a.id==='rune1')v+=.2*dps;if(a.id==='rune2')v+=.35*dps;if(a.id==='rune3')v+=.55*dps;
 if(a.id==='crit')v+=.25*dps;if(a.id.indexOf('chrono')===0)v+=.1*dps;if(a.id==='accelerator')v+=.1*dps;
 if(a.id==='vamp')v+=need?.5*dps:.1*dps;if(a.id==='amulet1')v+=need?.4*dps:.1*dps;
 if(a.id==='amulet2')v+=need?.6*dps:.1*dps;if(a.id==='stone')v+=need?.4*dps:.1*dps;
 if(a.id==='lens1')v+=.1*dps;if(a.id==='lens2')v+=.2*dps;if(a.id.indexOf('grim')===0)v+=.15*dps;
 if(['ember','lantern','conductor','prismc'].includes(a.id))v+=.2*dps;
 return v;}
```

**2.** Умная покупка — замените блок `while(buyIdx<BUY_ORDER.length){...}` на (ставить **после** расчёта `dps` и `gold`):
```js
  {const owned=new Set(boughtIds);let best=null,bestR=0;
   for(const a of ARTSHOP){if(a.craftOnly||owned.has(a.id))continue;if(a.req&&!owned.has(a.req))continue;
    const p=simPrice(a,t);if(p>gold)continue;
    const r=artValue(a,st,dps,hp/maxhp)/p;
    if(r>bestR){bestR=r;best=a;}}
   if(best){gold-=simPrice(best,t);best.apply(fakeG);maxhp=fakeG.player.maxhp;boughtIds.push(best.id);bought++;}}
```
(в начале `runSim` добавьте `const boughtIds=[];` и уберите `buyIdx`).

**3.** Жадный выбор магии — замените блок `if(level%3===0)st.dmg+=.2; else{...poolIdx...}` на:
```js
   if(level%4===0)st.dmg+=.2;
   else{let bestId=null,bestGain=0;
    for(const id in spells){const d=SPELL_DEF[id];if(d.combo)continue;const cur=spells[id];if(cur>=d.lv.length)continue;
     const g=spellDps(id,cur+1,t,st)-spellDps(id,cur,t,st);if(g>bestGain){bestGain=g;bestId=id;}}
    for(const id of C.pool){if(spells[id])continue;const g=spellDps(id,1,t,st);if(g>bestGain){bestGain=g;bestId=id;}}
    if(bestId)spells[bestId]=(spells[bestId]||0)+1;else st.dmg+=.2;}
```

---

### Что это даёт
- Сим теперь **сам собирает оптимальный билд**: качает заклинание с максимальным приростом DPS, покупает артефакт с лучшей ценностью/золото (DPS-веса, а выживаемость — только когда HP проседает), эволюционирует при готовности.
- Цены в симе = живым (`simPrice` с `1+t/900`) → калибровка честная, и «ЭПИК» в симе покажет, достижим ли он к концу.

Прогоните `node sim/node_runner.js` и пришлите `ИТОГ` + теплокарты + `артеф.~N` (теперь должно быть 4–8 осмысленных покупок). Если умный сим поднимет НОРМА к 60–75 при чистых картах — баланс можно фиксировать и переходить к **тревел-сцене** и **дереву синергий**.

---

## Ответ 4

Принято — цель по НОРМЕ закрыта (61 [B]), сим детерминированный и честный. Финалишу баланс (некромант-старт + хаос) и, как обещал после закрытия критериев, добавляю **тревел-сцену**: после босса — короткий шутер-пролёт «вправо» (авто-огонь, поток врагов навстречу), затем выбор артефакта и новая сцена жёстче. Дерево синергий — следующим шагом.

---

### `balance.js` (хаос мягче)
```js
  diffHp:.3,diffDmg:.1,diffSpawn:.15,diffElite:1.5},
```

### `spells.js` (некромант-старт)
```js
 soularrow:{name:'Стрела души',icon:'👻',cd:.95,lv:[{dmg:12,n:1,pierce:3},{dmg:18,n:2,pierce:4},{dmg:23,n:2,pierce:6},{dmg:33,n:3,pierce:8}]},
```
```js
 deathburst:{name:'Взрыв душ',icon:'💥',lv:[{dmg:20,aoe:56},{dmg:32,aoe:66},{dmg:48,aoe:78}]},
```

### `game.js` (тревел-сцена)
**1.** В объект `G` добавьте `travel:0,travT:0,autoT:0,`.

**2.** Босс → путешествие (вместо немедленной награды). Замените блок босса в `kill` на:
```js
 if(e.boss){G.bossRef=null;G.shake=12;sfx('boom');G.runBosses++;addGold(40);
  G.stage++;G.travel=12;G.travT=0;G.enemies=[];G.ebullets=[];banner('→ ПУТЕШЕСТВИЕ');}
```

**3.** В `director` первой строкой: `if(G.travel>0)return;`.

**4.** В `update` (после блока актов) добавьте логику пролёта:
```js
 if(G.travel>0){G.travel-=dt;
  G.travT-=dt;
  if(G.travT<=0){G.travT=.8;const ty=clamp(p.y+rnd(-220,220),20,WORLD-20);
   const b=BALANCE.enemies[pickArr(['runner','shooter','blob'])];
   G.enemies.push({x:p.x+W/2+60,y:ty,r:b.r,hp:b.hp*hpMul(),maxhp:b.hp*hpMul(),spd:0,dmg:b.dmg,xp:b.xp,goldV:b.gold,uid:uidN++,flash:0,stunT:0,slowT:0,slowM:1,kx:0,ky:0,dots:[],type:'runner',elite:false,boss:false,travel:true,vx:-rnd(150,210),shootT:1.5,tier:Math.min(4,G.stage+1)});}
  G.autoT-=dt;
  if(G.autoT<=0){G.autoT=.25;G.bullets.push({x:p.x+16,y:p.y,vx:520,vy:0,r:5,kind:'pierce',elem:'>',dmg:20*G.stats.dmg,pierce:2,life:1.2,hit:new Set(),col:'#fff'});}
  if(G.travel<=0){G.travel=0;G.enemies=[];G.ebullets=[];openReward(3);banner('✦ СЦЕНА '+(G.stage+1)+' — ЖЁСТЧЕ');}}
```

**5.** Движение тревел-врагов. В цикле врагов, перед `if(e.spd>0&&e.stunT<=0){`, добавьте:
```js
  if(e.travel){e.x+=e.vx*dt;
   if(e.type==='shooter'){e.shootT-=dt;if(e.shootT<=0){e.shootT=1.6;G.ebullets.push({x:e.x,y:e.y,vx:-220,vy:0,r:5,dmg:8,life:3});}}
   if(e.x<p.x-W){e.dead=true;}}
  else if(e.spd>0&&e.stunT<=0){ ... }
```
(т.е. оберните существующий чейз в `else`).

### `render.js` (эффект пролёта)
После `ctx.restore();` (в screen-space) добавьте:
```js
 if(G.travel>0){ctx.globalAlpha=.4;ctx.fillStyle='#eaf7ff';
  for(let i=0;i<16;i++){const y=(i*61+G.t*40)%H;const x=W-((G.t*520+i*137)%(W+120));ctx.fillRect(x,y,26,2);}
  ctx.globalAlpha=1;
  ctx.font='26px VT323, monospace';ctx.textAlign='center';ctx.fillStyle='#ffd166';ctx.fillText('→ ПУТЕШЕСТВИЕ →',W/2,90);}
```

---

### Итог
- Некромант стартует бодрее (soularrow/deathburst +10–25%), хаос ближе к 60.
- После каждого босса — **12-секундный шутер-пролёт**: авто-огонь вправо, встречные враги, полосы-скорость; затем выбор артефакта и следующая сцена с множителем `stage`.

Прогоните сим + живой тест. Следующим шагом — **визуальное дерево синергий** (ветки с гейтами от основной школы). Скажите «дальше» — продолжу.

---

## Ответ 5

Закрываю оба бага и отвечаю на вопросы отчёта (некро, ХАОС, лог, сервер).

---

## Баг 1: вкладки магазина заблокированы
Причина почти наверняка — `shopTab` объявлен **внутри** `renderShop` и сбрасывается на `'I'` при каждом клике. Вынесите его глобально и используйте этот `renderShop` целиком:

```js
let shopTab='I'; // ГЛОБАЛЬНО, до renderShop
function renderShop(){$('shopGoldBig').textContent='$ '+G.gold;
 $('shopNote').textContent='все артефакты · клик по карточке — рецепт · цены растут со временем';
 const tabs=$('shopTabs');tabs.innerHTML='';
 for(const[t,l]of[['I','ОБЫЧНОЕ'],['II','РЕДКОЕ'],['III','ЭПИК/КЛАСС']]){
  const b=document.createElement('button');b.className='ctab'+(shopTab===t?' active':'');b.textContent=l;
  b.onclick=()=>{shopTab=t;renderShop();};tabs.appendChild(b);}
 const box=$('shopItems');box.innerHTML='';const owned=new Set(G.artifacts);
 const inTab=a=>{const r=artRank(a);return shopTab==='I'?r==='I':shopTab==='II'?r==='II':(r==='III'||r==='КЛАСС'||r==='ТИР+');};
 for(const a of ARTSHOP){if(a.craftOnly||!inTab(a))continue;
  const p=shopPrice(a),has=owned.has(a.id),reqOk=!a.req||owned.has(a.req),can=!has&&reqOk&&G.gold>=p;
  const el=document.createElement('div');el.className='shopItem';
  el.innerHTML=`${a.req?'<div class="siTag">УЛУЧШ</div>':a.cls?'<div class="siTag">КЛАСС</div>':''}<div class="siIcon">${a.icon}</div><div class="siName">${a.name}</div><div class="siDesc">${a.desc}</div><button class="buyBtn" ${can?'':'disabled'}>${has?'ЕСТЬ':reqOk?p:'нужен '+(ARTSHOP.find(x=>x.id===a.req)||{}).name}</button>`;
  el.querySelector('.siIcon').onclick=()=>showArtRecipes(a);
  el.querySelector('.siName').onclick=()=>showArtRecipes(a);
  el.querySelector('.buyBtn').onclick=()=>{if(!can)return;G.gold-=p;if(a.req){const i=G.artifacts.indexOf(a.req);if(i>=0)G.artifacts.splice(i,1);}G.artifacts.push(a.id);if(a.keep)a.apply(G);recomputeStats();sfx('buy');refreshBuildUI();renderShop();};
  box.appendChild(el);}
 const cr=craftableRecipes();
 if(cr.length){const h=document.createElement('div');h.style.cssText='width:100%;text-align:center;color:#8ce99a';h.textContent='— СБОРКА —';box.appendChild(h);
  for(const r of cr){const res=ARTSHOP.find(x=>x.id===r.id);const el=document.createElement('div');el.className='shopItem';
   el.innerHTML=`<div class="siTag">СБОРКА</div><div class="siIcon">${r.icon}</div><div class="siName">${r.name}</div><div class="siDesc">${res.desc}</div><button class="buyBtn">СОБРАТЬ</button>`;
   el.querySelector('.buyBtn').onclick=()=>craftRecipe(r);box.appendChild(el);}}
 $('shopInfo').textContent='';}
```
(«Эпик» не заблокирован — у него может быть `req`-предмет; кнопка сама напишет, чего не хватает.)

## Баг 2: выбор артефакта зависает
Обновите `showArtRecipes` (пишет в `#shopInfo`, не баннером) и сделайте награду безопасной:
```js
function showArtRecipes(a){const rs=RECIPES.filter(r=>r.need.includes(a.id)||r.id===a.id);
 let s=a.icon+' '+a.name+': ';
 if(!rs.length)s+='вне сборок';
 else s+=rs.map(r=>{const miss=r.need.filter(n=>!G.artifacts.includes(n)).map(n=>(ARTSHOP.find(x=>x.id===n)||{}).name||n);
  return (r.id===a.id?'собирает '+r.name:'→ '+r.name)+(miss.length?' (нет: '+miss.join(', ')+')':' (готово)');}).join(' · ');
 $('shopInfo').textContent=s;}
function openReward(n){const owned=new Set(G.artifacts);
 const pool=ARTSHOP.filter(a=>!a.craftOnly&&!owned.has(a.id)&&(!a.req||owned.has(a.req))).sort(()=>Math.random()-.5).slice(0,n);
 if(!pool.length){addGold(60);banner('+60');return;}
 G.rewardOffers=pool;state='reward';$('rewardOv').classList.remove('hide');renderReward();}
function renderReward(){const box=$('rewardItems');box.innerHTML='';
 for(const a of G.rewardOffers){const el=document.createElement('div');el.className='shopItem';
  el.innerHTML=`<div class="siIcon">${a.icon}</div><div class="siName">${a.name}</div><div class="siDesc">${a.desc}</div><button class="buyBtn">ВЗЯТЬ</button>`;
  el.querySelector('.buyBtn').onclick=()=>{if(state!=='reward')return;G.artifacts.push(a.id);if(a.keep)a.apply(G);recomputeStats();refreshBuildUI();sfx('buy');closeReward();};
  box.appendChild(el);}}
function closeReward(){$('rewardOv').classList.add('hide');G.rewardOffers=[];if(state==='reward')state='play';}
```

---

## Ответы на вопросы отчёта

**Живой лог vs сим.** Расхождение закономерно: сим пессимистичен по раннему контакту (модель `hitRate`) и не кайтит; живой игрок двигается. Живой некро `en≈120` с 3-й мин = AoE-затык — это подтверждает и сим (▼ у некро). Модель в целом сходится по порядку, корректировать её дальше не нужно.

**Некро (вернуть soularrow lv1 13 + усилить AoE).** Да, в `spells.js`:
```js
 soularrow:{...lv:[{dmg:13,n:1,pierce:3},{dmg:18,n:2,pierce:4},{dmg:23,n:2,pierce:6},{dmg:33,n:3,pierce:8}]},
```
```js
 deathburst:{name:'Взрыв душ',icon:'💥',lv:[{dmg:20,aoe:64},{dmg:32,aoe:76},{dmg:48,aoe:90}]},
```
```js
 plague:{...lv:[{dps:12,r:62,dur:3},{dps:21,r:74,dur:3.6},{dps:32,r:88,dur:4.2}]},
```
Это поднимет кривую убийств некро, не трогая шторм.

**ХАОС 48 → 55–60.** Живой ХАОС идёт через `diffMode`, а `BALANCE.diff*` влияет только на стенд — централизуйте. Добавьте в `balance.js`:
```js
 chaos:{hp:1.3,dmg:1.1,spawn:1.2,reward:1.5},
```
и в `game.js` замените захардкоженные множители `diffMode===2` на `BALANCE.chaos.*`: в `hpMul` (`base*=BALANCE.chaos.hp`), `damagePlayer` (`d*=BALANCE.chaos.dmg`), спавн `director` (делить на `chaos.spawn`), `gainXP/addGold` (`*chaos.reward`). Значения `1.3/1.1/1.2` дадут ~55–60.

**Лог.** Расширить стоит: в `recordRun` добавьте в `L.push` поля `vow:G.vow,daily:G.daily,stage:G.stage,mode:G.diffMode`. Снапшоты каждые 10с уже пишутся — достаточно. **Сервер:** переходите на `serve.py` вместо `start.bat` (сбор через `POST /api/log` в `Data\run-log.json`).

Примените фиксы багов + некро/хаос и прогоните сим и живой тест. Скажите «дальше» — беру **визуальное дерево синергий** (ветки с гейтами от основной школы).

---

## Ответ 6

Продолжаю по плану: закрываю главный пробел UX — **визуальное дерево развития** в паузе (ветка основной школы, синергии, эволюции + ТИР2, второстепенные школы с гейтом). Это отвечает на ранний запрос «в паузе непонятно, что качать, и как связаны синергии».

---

### `index.html`
**1.** В ряду вкладок паузы добавьте кнопку (после `arts`):
```html
<button class="tabbtn" data-tab="tree">⌘ ДЕРЕВО</button>
```
и панель:
```html
<div id="tab-tree" class="tabpane hide"></div>
```
**2.** В `render.js` массив показа вкладок расширьте: `['magic','combos','syn','stats','arts','tree']`.

**3.** CSS:
```css
.treeRoot{max-width:720px;margin:0 auto}
.treeBranch{margin:14px 0 4px}
.treeBranch.head{color:#fff;font-size:20px;border-bottom:1px dashed var(--line);padding-bottom:4px}
.treeCol{padding-left:14px;border-left:1px dashed rgba(234,247,255,.2)}
.treeNode{display:flex;gap:10px;align-items:baseline;padding:5px 8px;border-bottom:1px dashed rgba(234,247,255,.1);font-size:18px}
.treeNode .tnIcon{width:30px;text-align:center;color:#fff}
.treeNode .tnName{flex:1}
.treeNode .tnLv{color:var(--dim);font-size:15px;text-align:right}
.treeNode.own .tnName{color:#8ce99a}
.treeNode.ready .tnName{color:#ffd166}
.treeNode.lock{opacity:.4}
```

### `game.js` (или combos.js)
```js
function syHasCross(sy,k){return sy.need.some(n=>{if(n.type==='passive')return false;const ow=spellOwner(n.id);return ow&&ow!==k;});}
function buildTreePane(){const k=G.key,C=CLASSES[k],unl=crossUnlocked();let h='<div class="treeRoot">';
 h+=`<div class="treeBranch head">${C.icon} ${C.name} — основная школа</div><div class="treeCol">`;
 for(const id of C.pool){const d=SPELL_DEF[id],lv=G.spells[id]||0;
  h+=`<div class="treeNode ${lv?'own':'idle'}"><span class="tnIcon">${d.icon}</span><span class="tnName">${d.name}</span><span class="tnLv">${lv?R[lv-1]+'/'+'R'.replace('R','')+(d.lv.length):'—'}</span></div>`;}
 h+='</div>';
 h+=`<div class="treeBranch head">~ Синергии</div><div class="treeCol">`;
 for(const sy of SYNERGIES){const own=synergyActive(sy),gated=!unl&&syHasCross(sy,k);
  const needs=sy.need.map(n=>{if(n.type==='passive')return PASSIVE_DEF[n.id].name+((G.passives[n.id]||0)?'✔':'·');
   const ow=spellOwner(n.id);return (ow&&ow!==k?'['+CLASSES[ow].letter+'] ':'')+SPELL_DEF[n.id].name+(((G.spells[n.id]||0)>=1)?'✔':'·');}).join(' + ');
  h+=`<div class="treeNode ${own?'own':gated?'lock':'idle'}"><span class="tnIcon">${sy.icon}</span><span class="tnName">${sy.name}</span><span class="tnLv">${own?'АКТИВНА':gated?'🔒 эволюция/LV12':needs}</span></div>`;}
 h+='</div>';
 h+=`<div class="treeBranch head">⭐ Эволюции</div><div class="treeCol">`;
 for(const c of COMBOS){if(c.cls&&c.cls!==k)continue;const on=G.combosAcquired.has(c.id),rd=comboConditionsMet(c);
  const t2=c.need.every(n=>SPELL_DEF[n.id]&&SPELL_DEF[n.id].combo);
  const needs=c.need.map(n=>{if(n.type==='passive')return PASSIVE_DEF[n.id].name+' ур'+n.lv+(((G.passives[n.id]||0)>=n.lv)?'✔':'·');
   return SPELL_DEF[n.id].name+' ур'+n.lv+(((G.spells[n.id]||0)>=n.lv)?'✔':'·');}).join(' + ');
  h+=`<div class="treeNode ${on?'own':rd?'ready':'idle'}"><span class="tnIcon">${c.icon}</span><span class="tnName">${c.name}${t2?' · ТИР2':''}</span><span class="tnLv">${on?'ИЗУЧЕНА':rd?'⭐ ГОТОВА':needs}</span></div>`;}
 h+='</div>';
 h+=`<div class="treeBranch head">Второстепенные школы ${unl?'':'🔒'}</div><div class="treeCol">`;
 for(const ok of CLASS_ORDER){if(ok===k)continue;const OC=CLASSES[ok];
  for(const id of OC.pool){const d=SPELL_DEF[id],lv=G.spells[id]||0;
   h+=`<div class="treeNode ${lv?'own':unl?'idle':'lock'}"><span class="tnIcon">${d.icon}</span><span class="tnName">${d.name}</span><span class="tnLv">${lv?R[lv-1]:unl?'доступно':'🔒'}</span></div>`;}}
 h+='</div></div>';$('tab-tree').innerHTML=h;}
```

### `render.js` — вызов
В `buildPause` добавьте в конец:
```js
 buildTreePane();
```

---

### Итог
Вкладка **⌘ ДЕРЕВО** показывает единый маршрут: закрашенные зелёным изученные заклинания, жёлтая «⭐ ГОТОВА» эволюция, синергии с чек-листом компонентов (✔/·), и заблокированные второстепенные школы с причиной (нужна эволюция своей школы или LV12). Игрок сразу видит, **что качать и что с чем сочетается**.

Следующим могу сделать **мини-карту целей** (иконки «к эволюции осталось N») поверх дерева, либо вернуться к **ХАОС-полировке через `BALANCE.chaos`**. Скажите «дальше» — продолжу.

---

## Ответ 7

Отличная идея — превращаю «путешествие» в полноценный **шутер-сегмент в духе 1944/1945**: после волны с боссом и сундука режим меняется — вид сверху, ваш корабль `▲` идёт вверх навстречу потоку врагов, кружащих и стреляющих, с красивым космическим фоном и гипер-лучами. Заодно чиню баг с дропом после мини-боссов.

**Новый цикл:** волна → босс → сундук с артефактом → **гиперпрыжок-шутер** → новая сцена жёстче.

---

### `game.js`

**1.** В `startGame` добавьте поля (замените `travel:0,` на):
```js
mode:'play',shoot:null,pendingShooter:false,
```

**2.** Босс → сундук (замените блок босса в `kill`):
```js
 if(e.boss){G.bossRef=null;G.shake=12;sfx('boom');G.runBosses++;addGold(40);
  G.stage++;G.chests.push({x:e.x,y:e.y,golden:true,t:0,boss:true});
  banner('☠ БОСС ПОВЕРЖЕН — СУНДУК!');}
```

**3.** Мини-босс (страж) → лечилка + сундук (фикс бага). Замените блок стража в `kill`:
```js
 if(e.type==='guard'){G.hearts.push({x:e.x,y:e.y,t:0});
  G.chests.push({x:e.x,y:e.y,golden:Math.random()<.5,t:0});
  banner('✦ СТРАЖ ПАЛ');}
```

**4.** `openChest` (босс-сундук помечает переход):
```js
function openChest(ch){const CH=BALANCE.chests;
 if(ch.golden){if(ch.boss)G.pendingShooter=true;openReward(3);banner('¤ ВЫБОР АРТЕФАКТА');}
 else{const gold=CH.goldMin+Math.round(G.t*CH.goldPerMin);G.gold+=gold;banner('◊ +'+gold);}
 if(G.track)G.track.chests++;sfx('chest');refreshBuildUI();}
```

**5.** `closeReward` (после выбора — прыжок):
```js
function closeReward(){$('rewardOv').classList.add('hide');G.rewardOffers=[];
 if(state==='reward')state='play';
 if(G.pendingShooter){G.pendingShooter=false;startShooter();}}
```

**6.** Функции шутера (добавьте):
```js
function startShooter(){G.mode='shooter';
 G.enemies=[];G.ebullets=[];G.bullets=[];G.orbs=[];G.merchant=null;
 G.shoot={dist:0,goal:2600,t:0,warp:1.4};G.travT=0;G.autoT=0;
 banner('→ ГИПЕРПРЫЖОК →');sfx('time');}
function endShooter(){G.mode='play';G.shoot=null;
 G.enemies=[];G.ebullets=[];G.bullets=[];
 G.player.x=WORLD/2;G.player.y=WORLD/2;G.cam.x=WORLD/2;G.cam.y=WORLD/2;
 banner('✦ СЦЕНА '+(G.stage+1)+' — ЖЁСТЧЕ');sfx('level');}
function spawnShooterEnemy(){const p=G.player,type=pickArr(['blob','runner','shooter']),b=BALANCE.enemies[type];
 const x=clamp(p.x+rnd(-W/2,W/2)*.9,30,WORLD-30),y=p.y-H/2-rnd(40,160);
 const e={x,y,r:b.r,hp:b.hp*hpMul(),maxhp:b.hp*hpMul(),spd:0,dmg:b.dmg,xp:b.xp,goldV:b.gold,uid:uidN++,flash:0,stunT:0,slowT:0,slowM:1,kx:0,ky:0,dots:[],type,elite:false,boss:false,sh:true,shV:rnd(90,170),shA:rnd(30,90),shF:rnd(1.2,2.4),shPh:rnd(0,TAU),shootT:rnd(1,2),tier:Math.min(4,G.stage+1)};
 if(type==='runner'){e.shA*=1.6;e.shV*=1.2;}
 G.enemies.push(e);}
function updateShooter(dt){const p=G.player,s=G.stats;G.shoot.t+=dt;
 if(G.shoot.warp>0){G.shoot.warp-=dt;}
 else{G.shoot.dist+=dt*230;
  G.travT-=dt;if(G.travT<=0){G.travT=clamp(.9-G.shoot.t*.018,.4,.9);spawnShooterEnemy();}
  G.autoT-=dt;
  if(G.autoT<=0){G.autoT=.17;const dmg=20*s.dmg;
   G.bullets.push({x:p.x,y:p.y-22,vx:0,vy:-560,r:5,kind:'pierce',elem:'^',dmg,pierce:2,life:1.6,hit:new Set(),col:'#fff'});
   const ex=G.stats.raysBonus+(G.spells.ray?SPELL_DEF.ray.lv[G.spells.ray-1].n:0);
   if(ex>0){G.bullets.push({x:p.x-16,y:p.y-12,vx:-90,vy:-540,r:4,kind:'pierce',elem:'^',dmg:dmg*.7,pierce:1,life:1.4,hit:new Set(),col:'#9fd8ff'});
    G.bullets.push({x:p.x+16,y:p.y-12,vx:90,vy:-540,r:4,kind:'pierce',elem:'^',dmg:dmg*.7,pierce:1,life:1.4,hit:new Set(),col:'#9fd8ff'});}}}
 if(G.shoot.dist>=G.shoot.goal)endShooter();}
```

**7.** В `update` замените вызов `director(dt);` на:
```js
 if(G.mode==='shooter')updateShooter(dt);else director(dt);
```
и **удалите** старый блок `if(G.travel>0){...}` целиком.

**8.** Движение шутер-врагов: в цикле врагов замените ветку `if(e.travel){...} else if(e.spd>0...)` на:
```js
  if(e.sh){
   e.shPh+=dt*e.shF;e.x+=Math.sin(e.shPh)*e.shA*dt;
   if(G.shoot&&G.shoot.warp<=0)e.y+=e.shV*dt;
   e.x=clamp(e.x,20,WORLD-20);
   if(e.y>p.y+H/2+80)e.dead=true;
   else if((e.type==='shooter'||e.type==='runner')&&e.y<p.y+H/2){e.shootT-=dt;
    if(e.shootT<=0){e.shootT=rnd(1.3,2.2);const a=Math.atan2(p.y-e.y,p.x-e.x);
     G.ebullets.push({x:e.x,y:e.y,vx:Math.cos(a)*180,vy:Math.sin(a)*180,r:5,dmg:e.dmg*.8,life:3});}}
  }
  else if(e.spd>0&&e.stunT<=0){ /* существующий чейз/стрелок/целитель без изменений */ }
```

### `render.js`

**1.** Космический фон (добавьте функцию):
```js
function drawShooterBG(){const warp=G.shoot&&G.shoot.warp>0,dist=G.shoot?G.shoot.dist:0;
 const g=ctx.createLinearGradient(0,0,0,H);
 g.addColorStop(0,'#0b0b2e');g.addColorStop(.5,'#1b0b40');g.addColorStop(1,'#050512');
 ctx.fillStyle=g;ctx.fillRect(0,0,W,H);
 for(let i=0;i<60;i++){const x=(i*97.3)%W,sp=(i%3===0?2.2:1.2),y=((i*211)+dist*sp)%H,sz=(i%3===0?2:1);
  ctx.globalAlpha=.35+.45*((i*37)%10)/10;ctx.fillStyle='#cfe9ff';ctx.fillRect(x,y,sz,sz);}
 const n=warp?60:10;
 for(let i=0;i<n;i++){const x=(i*173+dist*3)%W,y=((i*61)+dist*(warp?8:4))%H;
  ctx.globalAlpha=warp?.5:.15;ctx.fillStyle='#bfe9ff';ctx.fillRect(x,y,warp?3:2,warp?40:14);}
 ctx.globalAlpha=1;}
```

**2.** В `render` сразу после базовой заливки `ctx.fillRect(0,0,W,H)` добавьте:
```js
 if(G.mode==='shooter')drawShooterBG();
```
Оберните ареновые фоны/пикапы в `if(G.mode!=='shooter'){ ... }`: блок пола/пыли/деко/границы и блок торговца/сундуков/сердец.

**3.** Корабль вместо `@`. Замените отрисовку игрока на:
```js
 if(G.mode==='shooter'){
  glyph('▲',p.x,p.y,36,1,20);
  ctx.globalAlpha=.6+.4*Math.sin(G.t*30);glyph('v',p.x,p.y+22,18,.8,8,'#ff9a3d');ctx.globalAlpha=1;
  const n=8,filled=Math.round(clamp(p.hp/p.maxhp,0,1)*n);
  glyph('['+'█'.repeat(filled)+'░'.repeat(n-filled)+']',p.x,p.y+34,12,.8);
 }else if(!(p.ifr>0&&(G.t*20|0)%2===0)){ /* существующая отрисовка @ */ }
```

**4.** Прогресс-бар прыжка — добавьте в конец `render` (screen-space):
```js
 if(G.mode==='shooter'&&G.shoot){const f=clamp(G.shoot.dist/G.shoot.goal,0,1);
  ctx.fillStyle='rgba(0,0,0,.5)';ctx.fillRect(W/2-150,22,300,12);
  ctx.fillStyle='#7ee8fa';ctx.fillRect(W/2-150,22,300*f,12);
  ctx.font='16px VT323, monospace';ctx.textAlign='center';ctx.textBaseline='middle';
  ctx.fillStyle='#fff';ctx.fillText('ГИПЕРПРЫЖОК '+Math.round(f*100)+'%',W/2,14);}
```

---

### Итог
- Волна по таймеру → **босс** → **сундук с выбором артефакта** → **гиперпрыжок-шутер** (корабль `▲`, космос, звёзды, гипер-лучи на входе) → новая сцена жёстче.
- В шутере вы **уничтожаете** врагов (авто-огонь вверх + доп. стволы от лучей), а не кайтите; враги летят навстречу, качаются синусом и стреляют.
- Мини-боссы теперь роняют **сердце + сундук** (баг закрыт).

Прогоните живой тест. Если шутер покажется слишком лёгким/плотным — подкручу `goal`/частоту спавна/урон авто-огня. Скажите «дальше» — продолжу (дерево синергий или полировка шутера).

---

## Ответ 8

Понял — гиперпрыжок делаю полноценной фазой игры, босс-файт эпичным и ограниченным, а синергии — полноценными цветными карточками. Вот батч.

**Новый цикл:** волна → **эпичный босс** (цветной, арена-граница, миньоны ≤12) → **лучи справа** + сундук → выбор артефакта → **«ПРИГОТОВЬСЯ К ГИПЕРПРЫЖКУ»** (вертикальные лучи + враги нарастают) → **длинный шутер-полёт** (паттерны врагов, мид-элита) → новая сцена.

---

### `game.js`

**1.** В `startGame` добавьте поля:
```js
beamFx:0,prep:0,bossArena:null,synShown:new Set(),synCard:null,
```

**2.** Спавн босса — арена. В `spawnBoss` перед `G.enemies.push(e);` добавьте:
```js
 G.bossArena={x:WORLD/2,y:WORLD/2,r:380};
```

**3.** Смерть босса — лучи справа, зачистка, сундук. Замените блок босса в `kill`:
```js
 if(e.boss){G.bossRef=null;G.bossArena=null;G.shake=14;sfx('boom');G.runBosses++;addGold(40);
  G.stage++;G.enemies=[];G.ebullets=[];G.beamFx=2;
  G.chests.push({x:e.x,y:e.y,golden:true,t:0,boss:true});
  banner('☠ БОСС ПОВЕРЖЕН');}
```

**4.** Кап миньонов при боссе. В `director` замените `if(G.spawnT<=0){` на:
```js
 const bossLock=G.bossRef&&G.enemies.length>=12;
 if(G.spawnT<=0&&!bossLock){
```

**5.** Граница арены + таймеры в `update`. После блока движения игрока добавьте:
```js
 if(G.bossRef&&G.bossArena){const ba=G.bossArena,dx=p.x-ba.x,dy=p.y-ba.y,d=Math.hypot(dx,dy);
  if(d>ba.r){p.x=ba.x+dx/d*ba.r;p.y=ba.y+dy/d*ba.r;}}
 G.beamFx=Math.max(0,(G.beamFx||0)-dt);
 if(G.prep>0){G.prep-=dt;
  G.travT-=dt;if(G.travT<=0){G.travT=.9;spawnShooterEnemy();}
  if(G.prep<=0){G.prep=0;startShooter();}}
```

**6.** Карточки синергий в `update` (после подсистем):
```js
 for(const sy of activeSynergies())if(!G.synShown.has(sy.id)){G.synShown.add(sy.id);G.synCard={sy,t:2.8};sfx('level');}
 if(G.synCard){G.synCard.t-=dt;if(G.synCard.t<=0)G.synCard=null;}
```

**7.** `closeReward` → подготовка к прыжку:
```js
function closeReward(){$('rewardOv').classList.add('hide');G.rewardOffers=[];
 if(state==='reward')state='play';
 if(G.pendingShooter){G.pendingShooter=false;G.prep=2.4;banner('ПРИГОТОВЬСЯ К ГИПЕРПРЫЖКУ');}}
```

**8.** Длинный, глубокий шутер. Замените `startShooter` и `spawnShooterEnemy`:
```js
function startShooter(){G.mode='shooter';
 G.ebullets=[];G.bullets=[];
 G.shoot={dist:0,goal:7000,t:0,warp:1.2,mid:false};G.travT=0;G.autoT=0;
 banner('→ ГИПЕРПРЫЖОК →');sfx('time');}
function spawnShooterEnemy(){const p=G.player,ph=G.shoot?G.shoot.dist/G.shoot.goal:0;
 const r=Math.random();let type,mode;
 if(r<.28){type='blob';mode='straight';}
 else if(r<.5){type='runner';mode='sine';}
 else if(r<.68){type='shooter';mode='hover';}
 else if(r<.85){type='runner';mode='dive';}
 else{type='brute';mode='sine';}
 const b=BALANCE.enemies[type];
 const x=clamp(p.x+rnd(-W/2,W/2)*.9,30,WORLD-30),y=p.y-H/2-rnd(40,180);
 const e={x,y,r:b.r,hp:b.hp*hpMul(),maxhp:b.hp*hpMul(),spd:0,dmg:b.dmg,xp:b.xp,goldV:b.gold,uid:uidN++,flash:0,stunT:0,slowT:0,slowM:1,kx:0,ky:0,dots:[],type,elite:false,boss:false,sh:true,shMode:mode,shV:rnd(90,180)+ph*70,shA:rnd(30,100),shF:rnd(1.2,2.6),shPh:rnd(0,TAU),shootT:rnd(1,2),tier:Math.min(4,G.stage+1)};
 if(mode==='dive')e.shV*=1.3;
 G.enemies.push(e);}
```

**9.** Паттерны движения. Замените ветку `if(e.sh){...}` на:
```js
  if(e.sh){
   e.shPh+=dt*e.shF;const m=e.shMode||'sine';
   if(m==='straight')e.y+=e.shV*dt;
   else if(m==='sine'){e.x+=Math.sin(e.shPh)*e.shA*dt;e.y+=e.shV*dt;}
   else if(m==='dive'){const a=Math.atan2(p.y-e.y,p.x-e.x);e.x+=Math.cos(a)*e.shV*1.3*dt;e.y+=Math.sin(a)*e.shV*1.3*dt;}
   else if(m==='hover'){if(e.y<p.y-160)e.y+=e.shV*dt;else e.x+=Math.sin(e.shPh)*e.shA*dt;}
   e.x=clamp(e.x,20,WORLD-20);
   if(e.y>p.y+H/2+80)e.dead=true;
   else if((e.type==='shooter'||m==='hover')&&e.y<p.y+H/2){e.shootT-=dt;
    if(e.shootT<=0){e.shootT=rnd(1.2,2);const a=Math.atan2(p.y-e.y,p.x-e.x);
     G.ebullets.push({x:e.x,y:e.y,vx:Math.cos(a)*190,vy:Math.sin(a)*190,r:5,dmg:e.dmg*.8,life:3});}}
  }
  else if(e.spd>0&&e.stunT<=0){ /* существующий чейз */ }
```

**10.** Мид-элита в прыжке. В `updateShooter` после `G.shoot.dist+=dt*230;` добавьте:
```js
  if(!G.shoot.mid&&G.shoot.dist>=G.shoot.goal*.5){G.shoot.mid=true;
   const b=BALANCE.enemies.brute,e={x:p.x,y:p.y-H/2-60,r:b.r*1.3,hp:b.hp*hpMul()*3,maxhp:b.hp*hpMul()*3,spd:0,dmg:b.dmg,xp:8,goldV:20,uid:uidN++,flash:0,stunT:0,slowT:0,slowM:1,kx:0,ky:0,dots:[],type:'brute',elite:true,boss:false,sh:true,shMode:'hover',shV:110,shA:70,shF:1.6,shPh:0,shootT:1,tier:4};
   G.enemies.push(e);banner('!! ЭЛИТА В ГИПЕРПРОСТРАНСТВЕ');}
```

### `render.js`

**1.** Цветной босс. Замените `if(e.boss)glyph('[[Ω]]',e.x,e.y,40,.98,18);` на:
```js
  if(e.boss)glyph('[[Ω]]',e.x,e.y,40,.98,18,'#ff5f6d');
```

**2.** Граница арены босса — добавьте в мировой отрисовке (внутри `ctx.save()`):
```js
 if(G.bossRef&&G.bossArena){const ba=G.bossArena;
  ctx.strokeStyle='rgba(255,95,109,.55)';ctx.lineWidth=2;ctx.setLineDash([9,9]);
  ctx.beginPath();ctx.arc(ba.x,ba.y,ba.r,0,TAU);ctx.stroke();ctx.setLineDash([]);}
```

**3.** Лучи справа + подготовка + карточка синергии — добавьте в screen-space в конец `render`:
```js
 if(G.beamFx>0){const a=clamp(G.beamFx/2,0,1);
  for(let i=0;i<10;i++){const y=H*(i+.5)/10,len=W*.6*a;
   ctx.globalAlpha=a*.5;ctx.fillStyle='#ffd166';ctx.fillRect(W-len,y-2,len,4);}
  ctx.globalAlpha=1;}
 if(G.prep>0){const f=1-G.prep/2.4;
  for(let i=0;i<14;i++){const x=(i+.5)/14*W,hgt=H*f*((i%3===0)?1:.7);
   ctx.globalAlpha=.25+.3*f;ctx.fillStyle='#bfe9ff';ctx.fillRect(x-2,H-hgt,4,hgt);}
  ctx.globalAlpha=1;ctx.font='40px VT323, monospace';ctx.textAlign='center';ctx.textBaseline='middle';
  ctx.fillStyle='#fff';ctx.shadowColor='#7ee8fa';ctx.shadowBlur=16;
  ctx.fillText('ПРИГОТОВЬСЯ К ГИПЕРПРЫЖКУ',W/2,H/2);ctx.shadowBlur=0;}
 const sc=$('synCard');
 if(G.synCard){const sy=G.synCard.sy;sc.classList.remove('hide');
  sc.innerHTML=`<div class="scIcon">${sy.icon}</div><div class="scName">~ ${sy.name}</div><div class="scDesc">${sy.desc}</div><div class="scNeeds">${sy.need.map(n=>{
   if(n.type==='passive')return `<span class="scNeed" style="color:#8ce99a">✦ ${PASSIVE_DEF[n.id].name}</span>`;
   const ow=spellOwner(n.id);return `<span class="scNeed" style="color:${ow?CLASSES[ow].color:'#fff'}">${SPELL_DEF[n.id].icon} ${SPELL_DEF[n.id].name}</span>`;}).join(' + ')}</div>`;}
 else sc.classList.add('hide');
```

**4.** Цветные компоненты в `buildSynergyGrid`: замените строку `needs` на:
```js
  const needs=sy.need.map(n=>{if(n.type==='passive')return `<span style="color:#8ce99a">✦ ${PASSIVE_DEF[n.id].name}</span>`;
   const ow=spellOwner(n.id);return `<span style="color:${ow?CLASSES[ow].color:'#fff'}">${SPELL_DEF[n.id].icon} ${SPELL_DEF[n.id].name}</span>`;}).join(' + ');
```
и вставьте `${needs}` в `evoNeed`.

### `index.html`
Добавьте оверлей карточки синергии и стили:
```html
<div id="synCard" class="hide"></div>
```
```css
#synCard{position:fixed;top:90px;left:50%;transform:translateX(-50%);z-index:30;
 background:rgba(0,0,0,.85);border:1px solid #8ce99a;box-shadow:0 0 24px rgba(140,233,154,.3);
 padding:14px 22px;text-align:center;max-width:480px}
#synCard .scIcon{font-size:34px;color:#8ce99a}
#synCard .scName{font-size:24px;color:#fff}
#synCard .scDesc{font-size:16px;color:var(--dim)}
#synCard .scNeeds{margin-top:6px;font-size:17px}
#synCard .scNeed{margin:0 6px}
```

---

### Итог
- **Босс**: цветной `[[Ω]]`, круговая арена-граница (побег ограничен), миньонов ≤12 — массой не задавят.
- **После босса**: золотые лучи справа + сундук → выбор артефакта → «ПРИГОТОВЬСЯ К ГИПЕРПРЫЖКУ» с нарастающими вертикальными лучами и врагами.
- **Гиперпрыжок** — полноценная фаза ~30с: паттерны (прямо/синус/пике/зависающие), мид-элита, прогресс-бар, гипер-фон.
- **Синергии**: при активации всплывает цветная карточка с иконкой, описанием и компонентами по школам; в дереве компоненты тоже цветные.

Прогоните живой тест. Если прыжок длинный/короткий — подкручу `goal` (7000) и частоту спавна. Скажите «дальше» — продолжу.

---

## Ответ 9

Отвечаю на все 5 вопросов баланса и реализую кандидатов. Решения: **авто-огонь с 0%** (шутер = бой с самого прыжка), **нарастание от stage + вынос параметров в `BALANCE.shooter`**, **залп у ховеров со stage≥2**, **prep-плотность оставляем**, **отчёт за прыжок в лог + ачивка**, плюс **экран-«междумир»**.

**Ответы по пунктам:**
1. **Авто-огонь 60% → скучно.** Включаю с 0% и ставлю мощность в зависимость от `stage` (`autoDmg+stage*8`). Первая треть полёта перестаёт быть «уклонялкой».
2. **Плотность/урон от stage.** Добавляю `stageHp/.stageDmg` и уплотнение спавна со stage. Параметры — в `BALANCE.shooter`.
3. **Ховеры.** На normal прицел ок; со `stage≥2` ховеры дают залп из 3 пуль веером.
4. **Prep-плотность** (2–3 врага) — оставляю, это хороший разгон.
5. **Отчёт за прыжок** — да: считаю убийства в полёте, баннер + телеметрия + ачивка «Гиперстранник» (3 прыжка).
6. **Междумир** — короткий 1.6с слайд «СЦЕНА N» вместо отдельного экрана.

---

### `balance.js` — секция шутера
```js
 shooter:{goal:7000,speed:230,spawnStart:2.2,spawnEnd:1.4,autoDmg:20,stageHp:.2,stageDmg:.15,warp:1.2},
```

### `game.js`

**1.** В `startGame` добавьте поля: `interT:0,jumps:0,`.

**2.** `startShooter` из BALANCE:
```js
function startShooter(){G.mode='shooter';const SH=BALANCE.shooter;
 G.ebullets=[];G.bullets=[];
 G.shoot={dist:0,goal:SH.goal,t:0,warp:SH.warp,mid:false,kills:0};G.travT=0;G.autoT=0;
 banner('→ ГИПЕРПРЫЖОК →');sfx('time');}
```

**3.** `updateShooter` (авто-огонь с 0%, спавн от stage):
```js
function updateShooter(dt){const p=G.player,s=G.stats,SH=BALANCE.shooter;G.shoot.t+=dt;
 if(G.shoot.warp>0){G.shoot.warp-=dt;}
 else{G.shoot.dist+=dt*SH.speed;
  const ph=clamp(G.shoot.dist/G.shoot.goal,0,1);
  G.travT-=dt;
  if(G.travT<=0){G.travT=lerp(SH.spawnStart,SH.spawnEnd,ph)/(1+G.stage*.15);spawnShooterEnemy();}
  G.autoT-=dt;
  if(G.autoT<=0){G.autoT=.17;const dmg=(SH.autoDmg+G.stage*8)*s.dmg;
   G.bullets.push({x:p.x,y:p.y-22,vx:0,vy:-560,r:5,kind:'pierce',elem:'^',dmg,pierce:2,life:1.6,hit:new Set(),col:'#fff'});
   const ex=G.stats.raysBonus+(G.spells.ray?SPELL_DEF.ray.lv[G.spells.ray-1].n:0);
   if(ex>0){G.bullets.push({x:p.x-16,y:p.y-12,vx:-90,vy:-540,r:4,kind:'pierce',elem:'^',dmg:dmg*.7,pierce:1,life:1.4,hit:new Set(),col:'#9fd8ff'});
    G.bullets.push({x:p.x+16,y:p.y-12,vx:90,vy:-540,r:4,kind:'pierce',elem:'^',dmg:dmg*.7,pierce:1,life:1.4,hit:new Set(),col:'#9fd8ff'});}}}
 if(G.shoot.dist>=G.shoot.goal)endShooter();}
```

**4.** `spawnShooterEnemy` (stage-масштаб):
```js
function spawnShooterEnemy(){const p=G.player,SH=BALANCE.shooter,ph=G.shoot?clamp(G.shoot.dist/G.shoot.goal,0,1):0;
 const r=Math.random();let type,mode;
 if(r<.28){type='blob';mode='straight';}else if(r<.5){type='runner';mode='sine';}
 else if(r<.68){type='shooter';mode='hover';}else if(r<.85){type='runner';mode='dive';}
 else{type='brute';mode='sine';}
 const b=BALANCE.enemies[type],stg=1+G.stage*SH.stageHp;
 const x=clamp(p.x+rnd(-W/2,W/2)*.9,30,WORLD-30),y=p.y-H/2-rnd(40,180);
 const e={x,y,r:b.r,hp:b.hp*hpMul()*stg,maxhp:b.hp*hpMul()*stg,spd:0,dmg:b.dmg*(1+G.stage*SH.stageDmg),xp:b.xp,goldV:b.gold,uid:uidN++,flash:0,stunT:0,slowT:0,slowM:1,kx:0,ky:0,dots:[],type,elite:false,boss:false,sh:true,shMode:mode,shV:rnd(90,180)+ph*70,shA:rnd(30,100),shF:rnd(1.2,2.6),shPh:rnd(0,TAU),shootT:rnd(1,2),tier:Math.min(4,G.stage+1)};
 if(mode==='dive')e.shV*=1.3;
 G.enemies.push(e);}
```

**5.** Залп ховеров в ветке `e.sh` (замените блок стрельбы):
```js
   else if((e.type==='shooter'||m==='hover')&&e.y<p.y+H/2){e.shootT-=dt;
    if(e.shootT<=0){e.shootT=rnd(1.2,2);const a=Math.atan2(p.y-e.y,p.x-e.x);
     if(m==='hover'&&G.stage>=2){for(let i=-1;i<=1;i++){const aa=a+i*.3;
      G.ebullets.push({x:e.x,y:e.y,vx:Math.cos(aa)*190,vy:Math.sin(aa)*190,r:5,dmg:e.dmg*.8,life:3});}}
     else G.ebullets.push({x:e.x,y:e.y,vx:Math.cos(a)*190,vy:Math.sin(a)*190,r:5,dmg:e.dmg*.8,life:3});}}
```

**6.** Учёт убийств прыжка: в `kill` добавьте строку:
```js
 if(G.mode==='shooter'&&G.shoot)G.shoot.kills++;
```

**7.** `endShooter` с отчётом + междумир:
```js
function endShooter(){const k=G.shoot?G.shoot.kills:0;G.jumps=(G.jumps||0)+1;
 G.mode='play';G.shoot=null;G.enemies=[];G.ebullets=[];G.bullets=[];
 G.player.x=WORLD/2;G.player.y=WORLD/2;G.cam.x=WORLD/2;G.cam.y=WORLD/2;G.interT=1.6;
 banner('✦ СЦЕНА '+(G.stage+1)+' — ЖЁСТЧЕ · прыжок: '+k+' уб.');sfx('level');
 if(G.telemetry)G.telemetry.push({jump:true,kills:k,stage:G.stage});}
```

**8.** Таймер междумира в `update`: `G.interT=Math.max(0,(G.interT||0)-dt);`.

**9.** Ачивка: в `ACH` добавьте `{id:'jumper',name:'Гиперстранник',desc:'3 гиперпрыжка',test:r=>(r.jumps||0)>=3}`, и в `recordRun` в объект `run` добавьте `jumps:G.jumps||0`.

### `render.js` — междумир-слайд
Добавьте в screen-space в конец `render`:
```js
 if(G.interT>0){const a=clamp(G.interT/1.6,0,1);
  ctx.fillStyle='rgba(0,0,0,'+(a*.85)+')';ctx.fillRect(0,0,W,H);
  ctx.globalAlpha=a;ctx.font='52px VT323, monospace';ctx.textAlign='center';ctx.textBaseline='middle';
  ctx.fillStyle='#fff';ctx.shadowColor='#7ee8fa';ctx.shadowBlur=20;
  ctx.fillText('СЦЕНА '+(G.stage+1),W/2,H/2);ctx.shadowBlur=0;ctx.globalAlpha=1;}
```

---

### Итог
Прыжок теперь **боевой с первой секунды**, масштабируется от `stage`, параметры тюнятся в `BALANCE.shooter` без кода, ведётся **отчёт за прыжок** (убийства → телеметрия/баннер), есть **ачивка** и **междумир-слайд**.

Прогоните сим + живой прыжок. Если со stage≥2 станет горячо — снижу `stageDmg .15→.1` или `spawnEnd`. Скажите «дальше» — продолжу.

---

## Ответ 10

Принято — виток 4 зелёный, все критерии закрыты, `autoDmg 24` и прицел-наводчик — верные решения (одно попадание по блобу и стабильные kills). Отвечаю на вопросы и даю ТЗ витка 5.

## Ответы на вопросы 1–4

**1. Подбор глифов.** Радиус **34→44** + **магнит**: в радиусе 90px глиф притягивается к кораблю (260px/с), подбирается на 44. Движение всё ещё нужно, но фрустрация «пролетел мимо» уходит. Оба числа — в `BALANCE.shooter`.

**2. Шанс дропа.** **12%→16%** + **гарантия (pity)**: если к 70% прыжка не выпало ни одного глифа — следующее убийство роняет принудительно. Так каждый прыжок даёт хотя бы одно решение.

**3. Гиперстраж.** **Да, приоритет-метку** — наводчик целится в стража, пока он жив. Это превращает его из «декорации» в реальную цель и даёт дроп/бонус в runtime, а не только в коде. 10 строк — бери.

**4. Порядок.** Подтверждаю: **виток 5 = прыжки × обеты × хаос**, **виток 6 = дерево синергий**. Сначала докручиваем только что построенный шутер мета-связями (малый, цельный шаг), потом отдельный большой UX-блок дерева.

---

## ТЗ: виток 5 — «Прыжки × Обеты × Хаос»

**Цель:** гиперпрыжок реагирует на обет и режим, чтобы забеги ощущались по-разному. Всё новое — параметры в `BALANCE.shooter` + хелпер `shootMods()`. Плюс три фикса из вопросов 1–3.

### 0. `balance.js` → `shooter` (заменить блок целиком)
```js
 shooter:{goal:7000,speed:230,spawnStart:1.9,spawnEnd:1.2,autoDmg:24,stageHp:.2,stageDmg:.15,warp:1.2,
  spiralAt:.6,bossAt:.85,dropChance:.16,boostDur:6,
  pickupR:44,magnetR:90,
  vowFuryDmg:1.3,vowSwiftSpd:1.25,vowSwiftRate:.85,vowGreedDrop:1.5,vowStoneShield:1,
  chaosSpawn:1.35,chaosHp:1.3,chaosDmg:1.2,chaosDrop:1.3},
```

### 1. `game.js` — хелпер модификаторов (добавь рядом со `startShooter`)
```js
function shootMods(){const SH=BALANCE.shooter,v=G.vow||'',ch=G.diffMode===2;
 return{dmg:v==='fury'?SH.vowFuryDmg:1,
  rate:v==='swift'?SH.vowSwiftRate:1,
  spd:v==='swift'?SH.vowSwiftSpd:1,
  drop:(v==='greed'?SH.vowGreedDrop:1)*(ch?SH.chaosDrop:1),
  hp:ch?SH.chaosHp:1,edmg:ch?SH.chaosDmg:1,spawn:ch?SH.chaosSpawn:1};}
```

### 2. `startShooter` — щит Стойкости, счётчик дропов, метка стража, баннер-тег
Заполни объект и добавь после него:
```js
 G.shoot={dist:0,goal:SH.goal,t:0,warp:SH.warp,mid:false,kills:0,drops:0,guard:null};
 if((G.vow||'')==='stone')G.shipShield=(G.shipShield||0)+SH.vowStoneShield;
 const tags=[];if(G.vow)tags.push(VOWS[G.vow]);if(G.diffMode===2)tags.push('ХАОС');
 banner('→ ГИПЕРПРЫЖОК'+(tags.length?' · '+tags.join(' · '):''));sfx('time');
```

### 3. `updateShooter` — модификаторы в огне и спавне
В начало функции: `const M=shootMods();`. Затем:
- урон авто-огня: `const dmg=(SH.autoDmg+G.stage*8)*s.dmg*M.dmg;`
- интервал огня: `G.autoT=.17*M.rate;`
- интервал спавна: `G.travT=lerp(SH.spawnStart,SH.spawnEnd,ph)/(1+G.stage*.15)/M.spawn;`

### 4. Скорость корабля (Обет Скорости) — в основном блоке движения игрока
Замени `const sp=P.speed*s.spdMul;` на:
```js
 const sp=P.speed*s.spdMul*(G.mode==='shooter'?shootMods().spd:1);
```

### 5. `spawnShooterEnemy` — хаос-прочность/урон
В объекте врага домножь: `hp:b.hp*hpMul()*stg*M.hp, maxhp:b.hp*hpMul()*stg*M.hp, dmg:b.dmg*(1+G.stage*SH.stageDmg)*M.edmg,` (добавь `const M=shootMods();` в начало функции).

### 6. Дроп: шанс + pity (вопрос 2)
В логике дропа при убийстве в прыжке:
```js
 if(G.mode==='shooter'&&G.shoot){const M=shootMods(),ph=G.shoot.dist/G.shoot.goal;
  const pity=(ph>=.7&&G.shoot.drops===0);
  if(pity||Math.random()<SH.dropChance*M.drop){G.shoot.drops++;spawnBoost(e.x,e.y);}}
```

### 7. Подбор глифов: радиус + магнит (вопрос 1)
В цикле подбора бустов:
```js
 for(const b of G.boosts){const d=Math.hypot(p.x-b.x,p.y-b.y);
  if(d<SH.magnetR){const a=Math.atan2(p.y-b.y,p.x-b.x);b.x+=Math.cos(a)*260*dt;b.y+=Math.sin(a)*260*dt;}
  if(d<SH.pickupR){ /* существующий подбор */ b.dead=true;}}
```

### 8. Приоритет по Гиперстражу (вопрос 3)
При спавне стража сохрани ссылку: `G.shoot.guard=e;`. В наводчике:
```js
 let tgt=(G.shoot.guard&&!G.shoot.guard.dead)?G.shoot.guard:nearestShooterEnemy();
```
В `endShooter` обнули `G.shoot.guard=null;` (до обнуления `G.shoot` — прочитай раньше). Страж-хаос: при спавне домножь его hp на `shootMods().hp`, за убийство в хаосе бонус `+40` золота сверх `+80`.

---

## Критерии приёмки (виток 5)
- `node --check game.js balance.js` чисто; полный цикл сцены 1 headless без ошибок.
- Обет **Стойкость**: прыжок стартует с `shipShield≥1`; **Ярость**: урон ствола ×1.3; **Скорость**: корабль и огонь быстрее; **Жадность**: глифов в среднем больше (16%×1.5).
- **Хаос**: врагов за прыжок ~+30–35%, они прочнее/больнее, глифов больше; страж толще и даёт бонус-золото.
- Гиперстраж **убивается** наводчиком в runtime (проверить `kills` стража и факт дропа), а не только по коду.
- Pity: в прогоне без дропов к 70% — принудительный глиф появляется.
- Магнит подтягивает глиф в радиусе 90, подбор на 44.
- Регресс обычных волн (40с) чист, stage 0.

## Как проверить
`node --check game.js balance.js` → headless полный цикл сцены 1 на **каждом обете + норма/хаос** (5 комбинаций) → замер: глифов за прыжок, щит на старте (stone), урон/скорость (fury/swift), плотность (хаос), факт убийства стража. Отчёт `15-ХХ_шутер-виток5_статус-новый.md`.

---

Порядок подтверждён: **виток 5 — этот**, затем **виток 6 — визуальное дерево синергий** (ветки по школам с цветными гейтами и карточками доступных сетов). Применяй, гоняй, присылай отчёт — зафиксирую в INDEX и дам дерево синергий.

---

## Ответ 11

Принято — виток 4 зелёный, все критерии закрыты, `autoDmg 24` и прицел-наводчик — верные решения (одно попадание по блобу и стабильные kills). Отвечаю на вопросы и даю ТЗ витка 5.

## Ответы на вопросы 1–4

**1. Подбор глифов.** Радиус **34→44** + **магнит**: в радиусе 90px глиф притягивается к кораблю (260px/с), подбирается на 44. Движение всё ещё нужно, но фрустрация «пролетел мимо» уходит. Оба числа — в `BALANCE.shooter`.

**2. Шанс дропа.** **12%→16%** + **гарантия (pity)**: если к 70% прыжка не выпало ни одного глифа — следующее убийство роняет принудительно. Так каждый прыжок даёт хотя бы одно решение.

**3. Гиперстраж.** **Да, приоритет-метку** — наводчик целится в стража, пока он жив. Это превращает его из «декорации» в реальную цель и даёт дроп/бонус в runtime, а не только в коде. 10 строк — бери.

**4. Порядок.** Подтверждаю: **виток 5 = прыжки × обеты × хаос**, **виток 6 = дерево синергий**. Сначала докручиваем только что построенный шутер мета-связями (малый, цельный шаг), потом отдельный большой UX-блок дерева.

---

## ТЗ: виток 5 — «Прыжки × Обеты × Хаос»

**Цель:** гиперпрыжок реагирует на обет и режим, чтобы забеги ощущались по-разному. Всё новое — параметры в `BALANCE.shooter` + хелпер `shootMods()`. Плюс три фикса из вопросов 1–3.

### 0. `balance.js` → `shooter` (заменить блок целиком)
```js
 shooter:{goal:7000,speed:230,spawnStart:1.9,spawnEnd:1.2,autoDmg:24,stageHp:.2,stageDmg:.15,warp:1.2,
  spiralAt:.6,bossAt:.85,dropChance:.16,boostDur:6,
  pickupR:44,magnetR:90,
  vowFuryDmg:1.3,vowSwiftSpd:1.25,vowSwiftRate:.85,vowGreedDrop:1.5,vowStoneShield:1,
  chaosSpawn:1.35,chaosHp:1.3,chaosDmg:1.2,chaosDrop:1.3},
```

### 1. `game.js` — хелпер модификаторов (добавь рядом со `startShooter`)
```js
function shootMods(){const SH=BALANCE.shooter,v=G.vow||'',ch=G.diffMode===2;
 return{dmg:v==='fury'?SH.vowFuryDmg:1,
  rate:v==='swift'?SH.vowSwiftRate:1,
  spd:v==='swift'?SH.vowSwiftSpd:1,
  drop:(v==='greed'?SH.vowGreedDrop:1)*(ch?SH.chaosDrop:1),
  hp:ch?SH.chaosHp:1,edmg:ch?SH.chaosDmg:1,spawn:ch?SH.chaosSpawn:1};}
```

### 2. `startShooter` — щит Стойкости, счётчик дропов, метка стража, баннер-тег
Заполни объект и добавь после него:
```js
 G.shoot={dist:0,goal:SH.goal,t:0,warp:SH.warp,mid:false,kills:0,drops:0,guard:null};
 if((G.vow||'')==='stone')G.shipShield=(G.shipShield||0)+SH.vowStoneShield;
 const tags=[];if(G.vow)tags.push(VOWS[G.vow]);if(G.diffMode===2)tags.push('ХАОС');
 banner('→ ГИПЕРПРЫЖОК'+(tags.length?' · '+tags.join(' · '):''));sfx('time');
```

### 3. `updateShooter` — модификаторы в огне и спавне
В начало функции: `const M=shootMods();`. Затем:
- урон авто-огня: `const dmg=(SH.autoDmg+G.stage*8)*s.dmg*M.dmg;`
- интервал огня: `G.autoT=.17*M.rate;`
- интервал спавна: `G.travT=lerp(SH.spawnStart,SH.spawnEnd,ph)/(1+G.stage*.15)/M.spawn;`

### 4. Скорость корабля (Обет Скорости) — в основном блоке движения игрока
Замени `const sp=P.speed*s.spdMul;` на:
```js
 const sp=P.speed*s.spdMul*(G.mode==='shooter'?shootMods().spd:1);
```

### 5. `spawnShooterEnemy` — хаос-прочность/урон
В объекте врага домножь: `hp:b.hp*hpMul()*stg*M.hp, maxhp:b.hp*hpMul()*stg*M.hp, dmg:b.dmg*(1+G.stage*SH.stageDmg)*M.edmg,` (добавь `const M=shootMods();` в начало функции).

### 6. Дроп: шанс + pity (вопрос 2)
В логике дропа при убийстве в прыжке:
```js
 if(G.mode==='shooter'&&G.shoot){const M=shootMods(),ph=G.shoot.dist/G.shoot.goal;
  const pity=(ph>=.7&&G.shoot.drops===0);
  if(pity||Math.random()<SH.dropChance*M.drop){G.shoot.drops++;spawnBoost(e.x,e.y);}}
```

### 7. Подбор глифов: радиус + магнит (вопрос 1)
В цикле подбора бустов:
```js
 for(const b of G.boosts){const d=Math.hypot(p.x-b.x,p.y-b.y);
  if(d<SH.magnetR){const a=Math.atan2(p.y-b.y,p.x-b.x);b.x+=Math.cos(a)*260*dt;b.y+=Math.sin(a)*260*dt;}
  if(d<SH.pickupR){ /* существующий подбор */ b.dead=true;}}
```

### 8. Приоритет по Гиперстражу (вопрос 3)
При спавне стража сохрани ссылку: `G.shoot.guard=e;`. В наводчике:
```js
 let tgt=(G.shoot.guard&&!G.shoot.guard.dead)?G.shoot.guard:nearestShooterEnemy();
```
В `endShooter` обнули `G.shoot.guard=null;` (до обнуления `G.shoot` — прочитай раньше). Страж-хаос: при спавне домножь его hp на `shootMods().hp`, за убийство в хаосе бонус `+40` золота сверх `+80`.

---

## Критерии приёмки (виток 5)
- `node --check game.js balance.js` чисто; полный цикл сцены 1 headless без ошибок.
- Обет **Стойкость**: прыжок стартует с `shipShield≥1`; **Ярость**: урон ствола ×1.3; **Скорость**: корабль и огонь быстрее; **Жадность**: глифов в среднем больше (16%×1.5).
- **Хаос**: врагов за прыжок ~+30–35%, они прочнее/больнее, глифов больше; страж толще и даёт бонус-золото.
- Гиперстраж **убивается** наводчиком в runtime (проверить `kills` стража и факт дропа), а не только по коду.
- Pity: в прогоне без дропов к 70% — принудительный глиф появляется.
- Магнит подтягивает глиф в радиусе 90, подбор на 44.
- Регресс обычных волн (40с) чист, stage 0.

## Как проверить
`node --check game.js balance.js` → headless полный цикл сцены 1 на **каждом обете + норма/хаос** (5 комбинаций) → замер: глифов за прыжок, щит на старте (stone), урон/скорость (fury/swift), плотность (хаос), факт убийства стража. Отчёт `15-ХХ_шутер-виток5_статус-новый.md`.

---

Порядок подтверждён: **виток 5 — этот**, затем **виток 6 — визуальное дерево синергий** (ветки по школам с цветными гейтами и карточками доступных сетов). Применяй, гоняй, присылай отчёт — зафиксирую в INDEX и дам дерево синергий.

---
