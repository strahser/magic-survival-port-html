# ОТЧЁТ виток 10: «Попап карточки дерева» + баннер готовности — статус: новый

Дата: 2026-08-16 · Ветка: shooter-витки (magic-survival-port-html)
ТЗ: ответ Qwen (22:00, продолжение витка 9) — попап по клику в ⌘ ДЕРЕВО, баннер «★ ГОТОВО К СЛИЯНИЮ».

## Что сделано (код)
1. `game.js` — `mtCard` += `mtId` (6-й аргумент) и `data-mtid`; все 5 вызовов в `buildTreePane` получили id (`spell:<id>` / `syn:<id>` / `combo:<id>`).
2. `game.js` — `showMtPopup(key)`/`closeMtPopup()`: попап для заклинаний (уровни I/II/III через spellDesc, текущий подсвечен `.cur`), синергий (компоненты по школам с цветами, активна/нет, desc), эволюций/ТИР2 (требования с ✔/(cur), статус ИЗУЧЕНА/★/не готова, desc, метка ТИР2).
3. `game.js` — `checkReadyEvos()`: баннер «★ ГОТОВО К СЛИЯНИЮ: имя» один раз на эволюцию (G.readyEvoShown в startGame, вызов в конце `finishChoice()`).
4. `index.html` — оверлей `#mtPopup` (закрытие по фону и по кнопке) + CSS (.mpBox/.mpIcon/.mpName/.mpSub/.mpReq/.mpDesc/.mpLevels/.mppRow.cur).
5. `render.js` — делегирование клика на `#tab-tree` (.mtCard → showMtPopup(data-mtid)).

## Доказательства (headless Edge, errs:[])
- 48 карточек с data-mtid ✓; попапы:
  - spell: «Огненный шар / Пиромант · ур I / I: урон:14 снар:1 радиус:46 / II: ... / III: ...» ✓
  - syn: «Огненный вихрь / синергия · не активна / * Огненный шар + ✦ Поступь ветра / +25% область» ✓
  - combo: «Солнечная буря / эволюция · не готова / * Огненный шар ур 4 (1) + o Кольцо магмы ур 3 (0) / Шары+новы огня.» ✓
- Закрытие: popupHidden=true (по кнопке; фон — тот же обработчик) ✓
- Баннер готовности: при fireball ур4 + ring ур3 → «★ ГОТОВО К СЛИЯНИЮ: Солнечная буря», readyEvoShown=1 (один раз) ✓
- node --check game.js/render.js: OK; errs: [] во всех прогонах.

## Регресс
Дерево (5 секций, mtReadyBar), гипердерево ⚡, глиф-комбо, бой — код не затрагивался (только добавления); errs: [].

## Открытые вопросы (виток 11)
1. По ТЗ Qwen виток 11 = глиф-комбо ТИР2 (▲▲×3 = перегрев, ≡×3 = буря и т.п.). Подтверждаешь старт?
2. Баннер готовности показывается только после прокачки (finishChoice): показать также при входе в паузу (в ⌘ ДЕРЕВО счётчик уже есть) или достаточно?