# ОТЧЁТ виток 11: «Глиф-комбо ТИР2» — статус: новый

Дата: 2026-08-16 · Ветка: shooter-витки (magic-survival-port-html)
ТЗ: ответ Qwen (22:30, продолжение витка 10) — 3 глифа = ТИР2, накопительный счёт, суперсед.

## Что сделано (код)
1. `balance.js` — `shooter.combo` = `{need:2, need2:3, rateDur:5, spreadDur:5, spreadBarrels:4, shieldBonus:2, shieldHeal:10, rate2Dur:4, rate2Mult:3, spread2Dur:4, spread2Barrels:6, shield2Bonus:3, shield2Heal:25}`.
2. `game.js`:
   - `startShooter`: `G.glyphCount={rate,spread,shield}`, `G.glyphTier={}`, `G.comboBoost={rate,rateMult:2,spread,spreadBarrels:4,shieldVis}` (сброс каждый прыжок).
   - Подбор: накопительный счёт `glyphCount++` → `checkGlyphTier(t)` (без сброса счётчика).
   - `checkGlyphTier(t)`: при ≥3 и тир<2 → ТИР2; при ≥2 и тир<1 → ТИР1; каждый тир один раз за прыжок.
   - `triggerGlyphCombo(t,tier)`: ТИР1 (Гатлинг/Шторм-веер/Бастион как раньше), ТИР2: ▲▲×3 ПЕРЕГРЕВ (огонь ×3 на 4с, rateMult 3), ≡×3 БУРЯ (+6 стволов на 4с), ◯×3 КРЕПОСТЬ (+3 щит, +25 HP); ТИР2 — тряска (shake 6).
   - `updateShooter`: `autoT × (1/rateMult)` (2 → ×2 огонь, 3 → ×3); боковые `+comboBoost.spreadBarrels` (4 или 6).
3. `render.js` — индикатор тира: ▲▲ / ▲▲▲ (rateMult≥3), ≡ / ≡≡ (spreadBarrels≥6).

## Доказательства (headless Edge, принудительные дропы 3 глифов одного типа, errs:[])
- rate: ТИР1 @221 (Гатлинг) → ТИР2 @241 (ПЕРЕГРЕВ, rateMult=3 ✓); баннеры «▲▲×2 ГАТЛИНГ!» + «▲▲×3 ПЕРЕГРЕВ!» ✓.
- spread: «≡×2 ШТОРМ-ВЕЕР!» + «≡×3 БУРЯ!», spreadBarrels=6 ✓.
- shield: «◯×2 БАСТИОН!» + «◯×3 КРЕПОСТЬ!», shipShield 4+ (часть поглощена уроном), +25 HP ✓.
- Накопительный счёт: glyphCount.rate=4 после 3-го глифа — 4-й не спамит (glyphTier.rate=2, один триггер) ✓.
- Сброс: новый прыжок → glyphCount {0,0,0}, glyphTier {} ✓.
- Регресс: ТИР1 не сломан, errs: [] во всех прогонах; node --check OK.

## Открытые вопросы (виток 12)
1. По ТЗ Qwen виток 12 = один из: шутер-счёт (серии убийств ×дамаг), перестройка дерева синергий школы, полировка обетов (Скорость C41 / Стойкость D33 в симе). Что берём?
2. ТИР2-бонусы (×3 огонь 4с) при текущей плотности дропов (16%+pity): ×3 за прыжок набирается редко — норм (редкость = ценность) или поднять дроп при наличии тир-прогресса?